/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['fonts.googleapis.com', 'images.unsplash.com', 'images.unsplash.com'],
      },
    };
    
    export default nextConfig;